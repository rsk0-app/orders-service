apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-orders-service
  labels:
    app: orders-service
spec:
  selector:
    app: orders-service
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
