apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-orders-service
  labels:
    app: {{ .Values.appLabel | default "orders-service" }}
spec:
  selector:
    app: {{ .Values.appLabel | default "orders-service" }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
