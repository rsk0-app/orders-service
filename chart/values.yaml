replicaCount: 2
image:
  repository: ghcr.io/rsk0-app/orders-service
  tag: canary
  pullPolicy: Always
service:
  port: 80
  targetPort: 8080
resources:
  limits:
    memory: 128Mi
  requests:
    cpu: 50m
    memory: 64Mi
env:
  ENVIRONMENT: production
failure:
  mode: errors
  errorRate: "0.45"
  latencyMs: "1500"
