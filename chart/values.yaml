replicaCount: 2
image:
  repository: ghcr.io/rsk0-app/orders-service
  tag: latest
  pullPolicy: Always
service:
  port: 80
  targetPort: 8080
resources:
  limits:
    memory: 128Mi
  requests:
    cpu: 50m
    memory: 64Mi
env:
  ENVIRONMENT: production
failure:
  mode: ""
  errorRate: "0"
  latencyMs: "0"
